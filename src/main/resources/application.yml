---
  server:
    port: 8080

  spring:
    datasource:
      url: jdbc:postgresql://postgres/crazy-task-tracker
      username: postgres
      password: postgres

    jpa:
      hibernate:
        ddl-auto: update

      show-sql: true

      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQL.dialect